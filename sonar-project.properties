# must be unique in a given SonarQube instance
sonar.projectKey=js-methods
# --- below all are the optional properties ---
# defaults to project key
sonar.projectName=js-methods
#sonar.login=username
#sonar.password=pass
# defaults to 'not provided'
sonar.projectVersion=1.0
# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=src
sonar.tests=test
sonar.test.inclusions=**/*.test.js
sonar.javascript.lcov.reportPaths=coverage/lcov.info
sonar.exclusions=**/node_modules/**/*, **/*.md, **/*.json, .git/**/*, .config/**/*, .config/**/*, coverage/**/*, sonar-project.properties,
